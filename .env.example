# SPARK — environment configuration
# Copy to `.env.local` for local development. Never commit secrets.

# Contact form delivery destination (used from Stage 8 onward).
# Leave empty until a delivery provider/endpoint is configured. When empty,
# the contact form fails gracefully instead of submitting nowhere.
CONTACT_FORM_TO=